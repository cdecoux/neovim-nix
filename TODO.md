
- Add this repo into the main os-config for initial setup. Prefer a prebuilt from `main`. Pull the repo to $HOME and alias a command to `nix run $HOME/nvim`
- Configure https://github.com/knubie/vim-kitty-navigator ? Or something to easier to move panes using current kitty bindings I use
- Configure keybindings for basic file editing familiarity (Ctrl+S)
